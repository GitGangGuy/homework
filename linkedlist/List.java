package linkedlist;

import java.utils.Arrays.copyOf;

final public class List<T> {
    private T[] objects;

    public List() {
        objects = new T[0];
    }

    public void add(T item) {
        objects = objects.copyOf()
        objects = new T[objects.length];
    }
}